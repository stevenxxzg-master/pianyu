# PianYu MVP 发布验收报告

## 验收基线

- 验收日期：2026-03-09
- 验收对象：当前可部署 `origin/main` / `HEAD` `eb848d082a`
- 判定原则：只以当前主干已落地的仓库内容、测试结果和 GitHub / Linear 现状为准；未合并 PR 不计入已交付能力
- 总结论：**不通过（Not Ready）**

当前主干已经能证明注册、登录、进入首页、默认关系流入口、短文本发布，以及通知基础链路是可工作的；但发布验收仍未通过，因为发布保障能力和发布前置资产没有完整落到当前主干：`MAS-39` 和 `MAS-40` 的 PR 仍处于 `OPEN`，同时主干缺少生产部署、监控、备份/回滚、性能基线等 reviewer 可直接核对的仓库内文档或脚本。

## 验收明细

| 验收项                                     | 结论     | 证据                                                                                                                                                                                                                                                                                                                                                                                                                                            | 说明                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 新用户注册 / 登录 / 进入首页               | 通过     | `spec/system/auth/registrations_spec.rb`、`spec/system/log_in_spec.rb`、`spec/system/home_spec.rb`；`mise exec ruby@3.4.8 node@24.14.0 -- bundle exec rspec spec/system/auth/registrations_spec.rb spec/system/log_in_spec.rb spec/system/home_spec.rb spec/system/new_statuses_spec.rb` 跑出 `10 examples, 0 failures`                                                                                                                         | 该命令证明当前主干的非 `:js` 核心认证路径可通过；`mise exec` 显式使用仓库 pin 的 Ruby / Node 版本                                                                                                                                                                                                                                                                                             |
| 首页默认进入关注流                         | 通过     | `app/javascript/mastodon/features/ui/index.jsx`、`app/javascript/mastodon/features/home_timeline/index.jsx`、`app/models/user_settings.rb`                                                                                                                                                                                                                                                                                                      | 已登录用户在默认单栏布局下从 `/` 跳转到 `/home`；`web.advanced_layout` 默认值为 `false`，所以默认入口不是 `Explore`                                                                                                                                                                                                                                                                           |
| 快速发一条短文本                           | 通过     | `spec/system/new_statuses_spec.rb`；`mise exec ruby@3.4.8 node@24.14.0 -- bundle exec rspec spec/system/new_statuses_spec.rb --tag js --tag streaming` 跑出 `1 example, 0 failures`                                                                                                                                                                                                                                                             | 明确验证了 compose 表单可以发布短文本并在页面看到回流结果                                                                                                                                                                                                                                                                                                                                     |
| 回复 / 关注 / 收藏 / 通知                  | 部分通过 | 回复：`spec/services/post_status_service_spec.rb`、`app/controllers/api/v1/statuses_controller.rb`、`app/javascript/mastodon/actions/compose.js`；关注：`spec/requests/api/v1/accounts_spec.rb`、`app/services/follow_service.rb`；收藏：`spec/requests/api/v1/statuses/bookmarks_spec.rb`、`spec/requests/api/v1/bookmarks_spec.rb`；通知：`spec/requests/api/v2/notifications_spec.rb`、`spec/system/streaming/channel_subscriptions_spec.rb` | `mise exec ruby@3.4.8 node@24.14.0 -- bundle exec rspec spec/requests/api/v2/notifications_spec.rb` 跑出 `29 examples, 0 failures`；`mise exec ruby@3.4.8 node@24.14.0 -- bundle exec rspec spec/system/streaming/channel_subscriptions_spec.rb --tag streaming` 跑出 `6 examples, 0 failures`。但当前主干没有专用 release smoke 套件把 reply / follow / bookmark / notification 串成单一路径 |
| 发现 / 推荐不会压过关系流                  | 通过     | `app/javascript/mastodon/features/ui/index.jsx`、`app/javascript/mastodon/features/navigation_panel/index.tsx`、`app/javascript/mastodon/features/explore/index.tsx`、`app/javascript/mastodon/features/ui/components/navigation_bar.tsx`                                                                                                                                                                                                       | 已登录默认入口不是 `/explore`；`Explore` 和 `Suggestions` 是独立路由。高级多栏布局的 `/deck/getting-started` 会展示趋势模块，但它不是默认新用户入口，也不是 `/explore` 本身                                                                                                                                                                                                                   |
| 生产环境具备监控 / 备份 / 回滚方案且有证据 | 不通过   | 已有基础能力：`config/initializers/prometheus_exporter.rb`、`config/initializers/opentelemetry.rb`、`app/controllers/health_controller.rb`、`docker-compose.yml`、`app/services/backup_service.rb`；缺失关键落地物：`docs/PRODUCTION.md`、`docs/PRODUCTION_MONITORING.md`、`docs/ops/backup-rollback-upgrade.md`、`docs/RELEASE_PERFORMANCE_BASELINE.md`                                                                                        | 当前主干能证明存在健康检查、指标埋点和用户导出备份，但不能证明已经具备 reviewer 可执行的生产监控、整站备份恢复、回滚方案和性能基线                                                                                                                                                                                                                                                            |
| 前置发布工单已真正落地到当前版本           | 不通过   | `gh pr view 27 --json state,mergedAt,url,title,headRefName,baseRefName`、`gh pr view 28 --json state,mergedAt,url,title,headRefName,baseRefName`                                                                                                                                                                                                                                                                                                | `MAS-39` 对应 PR `#27`、`MAS-40` 对应 PR `#28` 仍为 `OPEN`，当前主干缺失 `spec/system/smoke/core_flows_spec.rb`、`bin/test-system-smoke`、`docs/RELEASE_PERFORMANCE_BASELINE.md` 等前置资产                                                                                                                                                                                                   |

## 本次执行的验证

- `git fetch origin --prune && git pull --ff-only origin main` → `Already up to date.`，当前 `HEAD` 仍为 `eb848d082a`
- `mise trust && mise install ruby@3.4.8 node@24.14.0` → 成功；`mise` 仅提示 runtimes 已安装但未写入 repo config，未阻塞显式 `mise exec`
- `mise exec ruby@3.4.8 node@24.14.0 -- bundle exec rspec spec/system/auth/registrations_spec.rb spec/system/log_in_spec.rb spec/system/home_spec.rb spec/system/new_statuses_spec.rb` → `10 examples, 0 failures`
- `mise exec ruby@3.4.8 node@24.14.0 -- bundle exec rspec spec/system/new_statuses_spec.rb --tag js --tag streaming` → `1 example, 0 failures`
- `mise exec ruby@3.4.8 node@24.14.0 -- bundle exec rspec spec/requests/api/v2/notifications_spec.rb` → `29 examples, 0 failures`
- `mise exec ruby@3.4.8 node@24.14.0 -- bundle exec rspec spec/system/streaming/channel_subscriptions_spec.rb --tag streaming` → `6 examples, 0 failures`
- `gh pr view 27 ...` / `gh pr view 28 ...` → 两个 prerequisite PR 都未合并

## 阻塞项

1. `MAS-39` 的 PR `#27` 尚未合并，当前主干没有发布前专用的 `spec/system/smoke/core_flows_spec.rb` 与 `bin/test-system-smoke`。
2. `MAS-40` 的 PR `#28` 尚未合并，当前主干没有 `docs/RELEASE_PERFORMANCE_BASELINE.md`，因此无法对照发布前性能 / 容量基线。
3. 当前主干缺少 `MAS-32` / `MAS-37` / `MAS-38` 预期落地的 operator-facing 文档和脚本（生产部署、监控基线、备份 / 恢复 / 回滚 runbook），因此“可稳定运行的软件”所要求的运维可核对证据不完整。

## 建议验收结论

截至 **2026-03-09**，PianYu 当前可部署主干 **不满足** `MAS-41` 的 MVP 发布验收标准。建议在相关前置 PR 真正合并并进入当前主干后，基于合并后的版本重新执行一次发布验收；尤其需要重新核对：

- 当前主干是否已包含 release smoke runner 与核心路径 smoke spec
- 当前主干是否已包含性能基线文档
- 当前主干是否已包含 reviewer 可直接执行或核对的监控 / 备份 / 回滚 runbook
