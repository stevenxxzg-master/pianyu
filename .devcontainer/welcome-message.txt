👋 Welcome to your PianYu Mastodon Dev Container!

🛠️ Your environment is fully setup with all the required software.

🧭 Read `docs/PIANYU_DEVELOPMENT.md` for the clone-to-login flow.

💥 Run `bin/dev` to start the application processes.

🔐 Sign in locally with `admin@localhost` / `mastodonadmin` after boot.

🥼 Run `RAILS_ENV=test bin/rails assets:precompile && RAILS_ENV=test bin/rspec` to run the test suite.
