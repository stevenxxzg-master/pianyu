web: sh -c 'PORT="${PORT:-3000}" RAILS_ENV="${RAILS_ENV:-development}" exec bundle exec puma -C config/puma.rb'
sidekiq: sh -c 'RAILS_ENV="${RAILS_ENV:-development}" exec bundle exec sidekiq'
stream: env PORT=4000 yarn workspace @mastodon/streaming start
vite: yarn dev
